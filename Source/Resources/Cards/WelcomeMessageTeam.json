{
    "type": "AdaptiveCard",
    "$schema": "http://adaptivecards.io/schemas/1.2.0/adaptive-card.json",
    "version": "1.2",
    "body": [
        {
            "type": "TextBlock",
            "size": "Medium",
            "weight": "Bolder",
            "text": "${staticStrings.welcomeCardHeader}"
        },
        {
            "type": "TextBlock",
            "text": "${staticStrings.welcomeCardChannelText1}",
            "wrap": true
        },
        {
            "type": "TextBlock",
            "text": "${staticStrings.welcomeCardChannelText2}",
            "wrap": true
        }
    ],
    "actions": [
        {
            "type": "Action.OpenUrl",
            "title": "${staticStrings.welcomeCardViewConsultsButton}",
            "url": "${deepLink}"
        }
    ]
}